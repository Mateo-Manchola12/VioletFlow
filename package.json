{
  "name": "violetflow",
  "version": "0.0.1",
  "description": "VioletFlow project",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run start --workspace client\"",
    "pkg": "node scripts/run-in-package.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "eslint": "^9.30.1",
    "eslint-config-prettier": "^10.1.5",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.13"
  }
}
